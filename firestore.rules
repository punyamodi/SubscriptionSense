// Emulator-only, allow all reads/writes.
// See docs/09-security-rules.md for production rules.
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} { allow read, write: if true; }
  }
}
